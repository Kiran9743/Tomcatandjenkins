#!/bin/bash

# Initialize counter
i=1

# While loop to iterate from 1 to 100
while [ $i -le 100 ]
do
  # Check if the number is even
  if [ $((i % 2)) -eq 0 ]
  then
    echo $i
  fi
  
  # Increment the counter
  i=$((i + 1))
done
